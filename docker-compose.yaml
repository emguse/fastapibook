version: '3'
services:
  demo-app:
    build: .
    volumes:
      - .dockervenv:/src/.venv
      - .:/src
    ports:
      - 8002:8002
    environment:
      #Required for hot reload in some environments.
      - WATCHFILES_FORCE_POLLING=true 
